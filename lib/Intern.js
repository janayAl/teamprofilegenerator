const Employee = require('./Employee');

class Manager extends Employee {
  constructor(School) {
    super(Employeename, id,  email);
    this.School = School; 
    this.getRole = () => {
      console.log (`Intern`)  //double check this part. Should return manager
  }
  this.getSchool = () => {
    console.log (`${this.school}`) 
}
}
}